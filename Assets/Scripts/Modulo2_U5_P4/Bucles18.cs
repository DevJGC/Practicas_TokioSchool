using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bucles18 : MonoBehaviour
{
    
    // Ejercicio 18 - Explicación de la diferencia entre While y For

    // While (valor booleano)
    // Ejemplo While:
    // While (i > 0) // Repetir mientras i sea mayor que 0
    // Y dentro del cuerpo que i sume o reste para mantener el bucle activo mientras se cumpla la operación


    // Sin embargo el bucle FOR incluye dentro la propia operación aritmetica, ejemplo:
    // For (int i=0 ; i < 10; i++) // Declara la variable temporal i y se ejecuta mientras i sea menor a 10 y además i suma 1 a cada ciclo




}
